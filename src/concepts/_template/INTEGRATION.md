# Integration recipe (template)
Replace with the target product's recipe: path, export style, route registration, data wiring, i18n keys.

The `components/` split (see `components/ExampleRow.tsx`) is neutral — map it to the product's own sub-component convention: pdfleader FSD `ui/` (+ `model/`, `lib/`, and an `index.ts` barrel), pdfguru `pages/<name>/parts`, tbp `pages/<name>/components`.
